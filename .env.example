# ============================================================
# CVG Line Maintenance Dashboard — Environment Variables
# ============================================================
# Copy this file to .env.local and fill in the values.
# Required variables are marked with [REQUIRED].
# ============================================================

# [REQUIRED] Auth.js session signing secret.
# Must be 32+ characters. Generate with: npm run generate-secret
AUTH_SECRET=

# Database file path. Default: data/dashboard.db (relative to project root)
# DATABASE_PATH=./data/dashboard.db

# Initial admin credentials for first-run seeding.
# If not set, seed will skip user creation — use /setup page instead.
# INITIAL_ADMIN_EMAIL=admin@example.com
# INITIAL_ADMIN_PASSWORD=

# Node environment. Set to "production" for deployment.
# NODE_ENV=production

# Enable the /api/seed endpoint. Default: disabled.
# In production, also requires superadmin auth.
# ENABLE_SEED_ENDPOINT=false
