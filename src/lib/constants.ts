/**
 * Shared constants — single source of truth for values used across the codebase.
 */

/** Auth ID for the internal system user (API ingest, CLI imports). */
export const SYSTEM_AUTH_ID = "00000000-0000-0000-0000-000000000000";

/** Email for the system user row. */
export const SYSTEM_USER_EMAIL = "system@internal";

/** Display name for the system user row. */
export const SYSTEM_USER_DISPLAY_NAME = "API Ingest";
