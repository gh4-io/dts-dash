# ============================================================
# DTSD Dashboard — Development Environment
# ============================================================
# Ready-to-use development configuration.
# Copy to .env.local: cp dev.env.local .env.local
# ============================================================

# ─── Authentication ─────────────────────────────────────────

# [REQUIRED] Auth.js session signing secret (32+ chars)
# This is a DEV-ONLY secret. Generate a new one for production:
#   npm run generate-secret
AUTH_SECRET=S6mCzagFpsYJ1zmLkK0qSWY0X6PADtaC0qhlCNFYXk9e5eA1lZqcD4689ZnDiybr

# ─── Database ───────────────────────────────────────────────

# SQLite database file path (relative to project root)
# Default: ./data/dashboard.db
DATABASE_PATH=./data/dashboard.db

# ─── First-Run Setup ────────────────────────────────────────

# Initial admin credentials for db:seed
# If not set, seed will skip user creation → use /setup page instead
INITIAL_ADMIN_EMAIL=admin@local
INITIAL_ADMIN_PASSWORD=admin123

# ─── Application Settings ───────────────────────────────────

# Node environment (automatically set by Next.js)
# Values: development | production | test
NODE_ENV=development

# ─── Note: Non-secret settings moved to server.config.yml ───
#
# The following settings are now configured in server.config.yml:
#   logging.level          (was LOG_LEVEL)
#   features.enableSeedEndpoint  (was ENABLE_SEED_ENDPOINT)
#   features.cronEnabled   (was CRON_ENABLED)
#   passwordSecurity.*     (was PASSWORD_* vars)
#
# Copy server.config.dev.yml → server.config.yml to get started.

# ─── Error Tracking (Optional) ──────────────────────────────

# Sentry DSN for error tracking (optional)
# Leave empty to disable error tracking
# SENTRY_DSN=

# ─── Development Notes ──────────────────────────────────────
#
# First-time setup:
#   1. Copy this file: cp dev.env.local .env.local
#   2. Reset database: npm run db:reset -- --seed
#   3. Start dev server: npm run dev
#   4. Login: admin@local / admin123
#
# Database scripts:
#   npm run db:seed          — seed demo data
#   npm run db:reset         — factory reset (delete DB + recreate)
#   npm run db:event-reset   — clear event data only
#   npm run db:status        — health check
#   npm run db:backup        — create backup
#
# Security reminders:
#   - Never commit .env.local to git
#   - Change admin password after first login
#   - Generate new AUTH_SECRET for production
#
