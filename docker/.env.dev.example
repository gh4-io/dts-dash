# ============================================================
# DTSD Dashboard — Development Environment
# ============================================================
# Ready-to-use development configuration.
#
# Setup:
#   cp docker/.env.dev.example .env.local
#   cp server.config.dev.yml server.config.yml
#   npm run dev
#
# Login: admin@local / admin123 (after first db:seed or db:reset --seed)
# ============================================================

# Auth.js session signing secret (DEV ONLY — generate a new one for production)
AUTH_SECRET=S6mCzagFpsYJ1zmLkK0qSWY0X6PADtaC0qhlCNFYXk9e5eA1lZqcD4689ZnDiybr

# SQLite database path (relative to project root)
DATABASE_PATH=./data/dashboard.db

# Auto-create first admin during db:seed
INITIAL_ADMIN_EMAIL=admin@local
INITIAL_ADMIN_PASSWORD=admin123

# Node environment
NODE_ENV=development

# ─── Application settings are in server.config.yml ────────────
# Copy the template: cp server.config.dev.yml server.config.yml
# Settings: logging.level, features.*, timeline.*, passwordSecurity.*, etc.
